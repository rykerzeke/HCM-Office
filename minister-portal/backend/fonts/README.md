# Fonts

For PDF export to use **Lora** font, place `Lora-Regular.ttf` here.

Download from [Google Fonts – Lora](https://fonts.google.com/specimen/Lora) (click "Download family", then extract and copy `Lora-Regular.ttf` into this folder).

If the file is missing, the PDF will use the default Helvetica font.
